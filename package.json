{
  "private": true,
  "name": "intiface",
  "scripts": {
    "build:all": "yarn workspace intiface-process-server build && yarn workspace intiface build && yarn workspace intiface electron:build"
  },
  "workspaces": [
    "packages/*"
  ]
}
